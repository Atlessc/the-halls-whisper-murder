import styles from './page.module.css'

export default function Home() {
  // create a function that checks if the game is rendered and if it is, it will render the game. until it is ready, it will render a loading screen
  
  
  return (
    <main className={styles.main}>
    </main>
  )
}
